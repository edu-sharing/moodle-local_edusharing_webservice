
services:
  mariadb:
    image: docker.io/bitnami/mariadb:latest
    environment:
      # ALLOW_EMPTY_PASSWORD is recommended only for development.
      - ALLOW_EMPTY_PASSWORD=yes
      - MARIADB_USER=bn_moodle
      - MARIADB_DATABASE=bitnami_moodle
      - MARIADB_CHARACTER_SET=utf8mb4
      - MARIADB_COLLATE=utf8mb4_unicode_ci
  moodle:
    image: dockerio.mirror.docker.edu-sharing.com/bitnami/moodle:4.5.1
    ## For local testing use build context:
    ##build:
    ##  context: .
    ports:
      - '80:8080'
      - '443:8443'
    environment:
      - BITNAMI_DEBUG=true
      - MOODLE_DATABASE_HOST=mariadb
      - MOODLE_DATABASE_PORT_NUMBER=3306
      - MOODLE_DATABASE_USER=bn_moodle
      - MOODLE_DATABASE_NAME=bitnami_moodle
      # ALLOW_EMPTY_PASSWORD is recommended only for development.
      - ALLOW_EMPTY_PASSWORD=yes
      - EDUSHARING_WEBSERVICE_USER=rendering
      - EDUSHARING_WEBSERVICE_PASSWORD=Rendering1#
      - EDUSHARING_REPOSITORY_PROT=https
      - EDUSHARING_REPOSITORY_PORT=443
      - EDUSHARING_REPOSITORY_HOST=repository.9-1.enterprise.123.45.67.89.nip.io
      - EDUSHARING_REPOSITORY_USERNAME=admin
      - EDUSHARING_REPOSITORY_PASSWORD=somepassword123
      - EDUSHARING_MOODLE_HOST=localhost
      - EDUSHARING_MOODLE_HOST_ALIASES=*
      - EDUSHARING_COURSE_MAX_SIZE=1024M
    depends_on:
      - mariadb